# 2026-07-29 开发日报：P3 主体完成 + 前端官网与市场页视觉升级

## 今日交付

### 提交清单

| 提交 | 类型 | 内容 |
|---|---|---|
| `3068181` | feat | P3 主体完成：packageRef 双模式 + 企业列表 |
| （未提交）| feat | 落地页官网 `/`（全新）|
| （未提交）| feat | 人才市场视觉升级 + 行业筛选 |
| （未提交）| style | globals.css 动效体系 |

### 验证数字

| 范围 | 结果 |
|---|---|
| 全量单元测试 | **155 / 155** |
| `npx tsc --noEmit`（后端）| 干净 |
| `npx tsc --noEmit`（前端）| 干净 |
| `next build` | **21 页全通** |

---

## 核心交付

### P3 主体完成

#### P3.1 员工包双模式发布
- Prisma schema：`packageRef JSONB` + 4 个 ZIP 字段改为可空，migration `20260729140000` 已部署
- `PackagePublishDtoSchema` 新增可选 `packageRef: { type: 'npm' | 'git', spec: string }`
- `publish()` 逻辑重排：文件校验提前到 DB 查询之前，支持 ZIP 或 packageRef 二选一，两者均为空时 `BadRequestException`
- 前端发布弹窗（`PublishPackageDialog`）加入 ZIP / packageRef 双模式切换

#### P3.2 实例包信息接口
- `GET /enterprise/instances/:id/package` 返回 `InstancePackageInfo`：`{ version, packageRef, zipAvailable, sha256 }`

#### P3.3 运营端企业列表
- 后端：`GET /enterprise/admin/all-enterprises`（`@Roles(ADMIN)` 守卫）
- 前端：`/admin/enterprises` 表格页，展示企业名、邮箱、成员数、订阅数、注册时间
- 平台 shell 导航加入"企业管理"入口

#### 单元测试修复
- `package.service.spec.ts` 花括号缺失修复；新增"file 和 packageRef 都为空时拒绝"用例
- 155 / 155 通过

---

### 前端视觉升级

#### 落地页官网（`/`）— 全新建立

| 板块 | 内容 |
|---|---|
| 导航栏 | Sticky，公司 logo（favicon.ico），产品功能 / 定价 / FAQ 锚点，登录 / 免费注册 |
| Hero | 点阵背景 + 双浮动光晕 orb + 徽章光泽动效 + 品牌渐变大标题 + 入场动画 |
| Stats | 8+ 行业 / 120+ 员工 / 500+ 企业，数字加粗突出 |
| Features | 6 张卡片：以组织为核心、员工化非工具化、开箱即用、权限可控、全程可追溯、多平台适配；hover 橙色光晕 |
| How It Works | 发现招聘→入职配置→授权开工；圆圈橙色渐变背景；桌面端虚线连接线 |
| Pricing | 体验版（免费）/ 专业版（¥2,999/月 · 最受欢迎）/ 企业版（联系销售）|
| FAQ | 5 条折叠式，client component |
| CTA | 渐变背景 + 转化文案 + 社会证明小字 |
| Footer | Logo + 导航链接 + 版权 |

动效体系（`globals.css` 新增）：
- `animate-float` / `animate-float-slow` — 元素漂浮
- `animate-fade-up` / `-d1 / -d2 / -d3` — 带延迟入场
- `shimmer-badge` — 光泽扫过
- `gradient-text-brand` — 品牌渐变文字
- `dot-grid-bg` — 点阵背景
- `card-glow` — hover 橙色光晕描边

#### 人才市场（`/marketplace`）— 三项升级

1. **首屏 Hero Banner**：橙色渐变背景，替换原来的 h1 标题区
2. **行业分类 Chips**：全部 / 电商零售 / 金融服务 / 医疗健康 / 教育培训 / 制造业 / 物流运输 / 餐饮服务 / 企业服务，点击切换客户端筛选
3. **员工卡片统计行**：能力数量 / 运行状态 / 版本号

---

## 待处理

- 前端视觉改动尚未 commit（可在确认效果后统一提交）
- P4 客户端接入层暂搁置，等客户端开发节奏确定后推进
- 落地页文案和定价数字为占位符，正式上线前需业务方确认
