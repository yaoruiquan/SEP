# SEP 客户端联调指南

**日期**: 2026-07-31  
**适用于**: SEP 客户端开发者（跨平台桌面客户端）  
**后端版本**: v0.1.0  
**API Base URL**: `http://localhost:3001`

---

## 📋 目录

1. [环境准备](#环境准备)
2. [联调账号](#联调账号)
3. [联调流程](#联调流程)
4. [关键接口测试](#关键接口测试)
5. [常见问题](#常见问题)
6. [调试技巧](#调试技巧)

---

## 环境准备

### 1. 后端服务启动

```bash
# 克隆仓库（如果还没有）
git clone <repository-url>
cd SEP

# 安装依赖
pnpm install

# 启动数据库
docker-compose up -d

# 初始化数据库
cd backend
npx prisma migrate deploy
npx prisma generate
npx prisma db seed

# 启动后端
npm run dev
```

**验证后端启动成功**:
```bash
curl http://localhost:3001/auth/login -X POST \
  -H "Content-Type: application/json" \
  -d '{"email":"staff@acme.local","password":"Demo123456"}'
```

预期返回：
```json
{
  "token": "eyJhbGc...",
  "user": {
    "id": "demo-user-acme-staff",
    "email": "staff@acme.local",
    "name": "技术员",
    "role": "USER"
  },
  "enterprise": {
    "id": "demo-ent-acme",
    "name": "示例科技有限公司"
  },
  "roleInEnterprise": "MEMBER"
}
```

### 2. 网络配置

**内网访问**:
- 后端服务: `http://192.168.10.199:3001`（替换为你的内网 IP）
- 确保客户端和后端在同一局域网或可互相访问

**CORS 配置**:
后端已配置允许所有来源（开发环境），生产环境需限制。

---

## 联调账号

### 测试企业：示例科技有限公司

**企业管理员**:
- 邮箱: `boss@acme.local`
- 密码: `Demo123456`
- 权限: 企业管理员（可管理所有员工和成员）

**普通员工**:
- 邮箱: `staff@acme.local`
- 密码: `Demo123456`
- 权限: 普通成员（可使用授权的员工）

### 可用的数字员工

| 员工名称 | ID | 行业 | 岗位 | 能力 |
|---------|---|------|------|------|
| 文案助手（技能包） | `demo-emp-writer-skill` | 电商 | 内容运营 | 写文案 |
| 市场调研员 | `demo-emp-market-researcher` | 跨境电商 | 市场 | 网页搜索+数据分析 |

---

## 联调流程

### 第一步：登录获取令牌

**接口**: `POST /auth/login`

**请求**:
```json
{
  "email": "staff@acme.local",
  "password": "Demo123456"
}
```

**响应**:
```json
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": { "id": "...", "email": "...", "name": "..." },
  "enterprise": { "id": "demo-ent-acme", "name": "示例科技有限公司" },
  "roleInEnterprise": "MEMBER"
}
```

**客户端需要保存**:
- `token` → 存储到本地（用于后续请求的 `Authorization: Bearer <token>`）
- `user.id` → 当前用户 ID
- `enterprise.id` → 当前企业 ID

---

### 第二步：获取可用员工实例列表

**接口**: `GET /client/instances`

**请求头**:
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**响应**:
```json
{
  "data": [
    {
      "id": "demo-inst-acme-writer",
      "name": "文案助手（技能包）",
      "avatar": "https://avatar.iran.liara.run/public/15",
      "description": "快速生成电商文案、广告语",
      "industry": "电商",
      "position": "内容运营",
      "employee": {
        "id": "demo-emp-writer-skill",
        "name": "文案助手（技能包）"
      }
    }
  ]
}
```

**客户端展示**:
- 在侧边栏或员工列表展示所有可用实例
- 用户可选择一个实例开始对话

---

### 第三步：创建会话

**接口**: `POST /client/conversations`

**请求**:
```json
{
  "instanceId": "demo-inst-acme-writer"
}
```

**响应**:
```json
{
  "data": {
    "id": "conv-abc123",
    "instanceId": "demo-inst-acme-writer",
    "createdAt": "2026-07-31T08:00:00Z"
  }
}
```

**客户端**:
- 保存 `conversationId`
- 打开聊天窗口

---

### 第四步：发送消息并接收流式响应

**接口**: `POST /gateway/chat`

**请求**:
```json
{
  "conversationId": "conv-abc123",
  "message": "帮我写一条母婴产品的种草文案"
}
```

**响应**: SSE 流式输出

```
data: {"type":"start"}

data: {"type":"text","content":"🎯 宝宝的第一口"}

data: {"type":"text","content":"辅食，选对了吗"}

data: {"type":"text","content":"？\n\n我家"}

data: {"type":"text","content":"宝宝6个月"}

...

data: {"type":"done","metadata":{"tokensUsed":150}}
```

**客户端处理**:
1. 使用 `EventSource` 或 `fetch` 接收 SSE 流
2. 逐块解析 `data:` 行
3. 根据 `type` 更新 UI:
   - `start` → 显示加载状态
   - `text` → 追加文本到消息气泡
   - `tool_call` → 显示"正在调用工具..."
   - `tool_result` → 显示工具返回结果
   - `done` → 完成，显示 token 消耗

---

### 第五步：获取会话历史

**接口**: `GET /client/conversations/:conversationId/messages`

**响应**:
```json
{
  "data": [
    {
      "id": "msg-1",
      "role": "user",
      "content": "帮我写一条母婴产品的种草文案",
      "createdAt": "2026-07-31T08:00:00Z"
    },
    {
      "id": "msg-2",
      "role": "assistant",
      "content": "🎯 宝宝的第一口辅食，选对了吗？...",
      "createdAt": "2026-07-31T08:00:05Z"
    }
  ]
}
```

**客户端**:
- 切换会话时加载历史消息
- 展示完整对话记录

---

## 关键接口测试

### 测试清单

用 curl 或 Postman 逐个测试：

#### 1. 登录
```bash
curl -X POST http://localhost:3001/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"staff@acme.local","password":"Demo123456"}'
```

✅ 预期：返回 `token` 和用户信息

#### 2. 获取实例列表
```bash
TOKEN="eyJhbGc..." # 替换为实际 token

curl http://localhost:3001/client/instances \
  -H "Authorization: Bearer $TOKEN"
```

✅ 预期：返回至少 2 个实例

#### 3. 创建会话
```bash
curl -X POST http://localhost:3001/client/conversations \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"instanceId":"demo-inst-acme-writer"}'
```

✅ 预期：返回新会话 ID

#### 4. 发送消息（流式）
```bash
CONV_ID="conv-abc123" # 替换为实际会话ID

curl -X POST http://localhost:3001/gateway/chat \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"conversationId":"'$CONV_ID'","message":"你好"}' \
  --no-buffer
```

✅ 预期：看到 SSE 流式输出

#### 5. 获取会话历史
```bash
curl http://localhost:3001/client/conversations/$CONV_ID/messages \
  -H "Authorization: Bearer $TOKEN"
```

✅ 预期：返回对话历史

---

## 常见问题

### Q1: 401 Unauthorized

**原因**:
- Token 过期（有效期 1 小时）
- Token 格式错误
- 未传 `Authorization` 头

**解决**:
```bash
# 重新登录获取新 token
curl -X POST http://localhost:3001/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"staff@acme.local","password":"Demo123456"}'
```

---

### Q2: 403 Forbidden - 无权访问员工实例

**原因**:
- 实例未授权给当前用户
- 用户所在部门未授权

**解决**:
1. 使用管理员账号登录 Web 前端（`http://localhost:3000`）
2. 进入「我的员工」→ 点击员工卡片 → 点击「授权」
3. 选择成员或部门，保存
4. 客户端重新获取实例列表

---

### Q3: 404 Not Found - 实例不存在

**原因**:
- `instanceId` 错误
- 实例已被删除

**解决**:
- 调用 `GET /client/instances` 获取最新列表
- 检查 `instanceId` 拼写

---

### Q4: SSE 流式响应中断

**原因**:
- 网络不稳定
- 代理服务器缓冲
- 超时设置过短

**解决**:
- 设置更长的超时时间（推荐 60s）
- 禁用代理缓冲
- 断线重连机制：
  ```javascript
  const eventSource = new EventSource(url);
  eventSource.onerror = () => {
    eventSource.close();
    // 3秒后重试
    setTimeout(() => reconnect(), 3000);
  };
  ```

---

### Q5: 消息发送后无响应

**排查步骤**:
1. 检查后端日志：`backend/logs/` 或控制台
2. 检查 sub2api 配置：`backend/.env` 中的 `SUB2API_API_KEY`
3. 测试 sub2api 连通性：
   ```bash
   curl https://longdaoai.cn/v1/models \
     -H "Authorization: Bearer sk-your-key"
   ```

---

### Q6: 算力不足

**错误信息**:
```json
{
  "error": "Insufficient compute balance",
  "code": "INSUFFICIENT_BALANCE"
}
```

**解决**:
1. 使用管理员账号登录 Web 前端
2. 进入「运营端」→「企业管理」
3. 找到测试企业，点击「充值」
4. 充值 1000 点算力

---

## 调试技巧

### 1. 开启详细日志

**后端**:
```bash
# 在 backend/.env 添加
LOG_LEVEL=debug
```

重启后端，查看详细请求日志。

### 2. 使用 Postman

导入 Swagger 文档：
1. 访问 `http://localhost:3001/api` (Swagger UI)
2. 下载 OpenAPI JSON
3. 导入到 Postman
4. 配置环境变量：
   - `baseUrl` = `http://localhost:3001`
   - `token` = `<登录后获取的token>`

### 3. 监控 SSE 流

使用 Chrome DevTools:
1. 打开 Network 面板
2. 筛选 `EventStream` 类型
3. 查看 `chat` 请求的响应流

### 4. 数据库查询

如果需要直接查看数据：
```bash
cd backend
npx prisma studio
```

打开 `http://localhost:5555` 查看数据库内容。

### 5. 测试工具推荐

**API 测试**:
- [Postman](https://www.postman.com/)
- [Insomnia](https://insomnia.rest/)
- curl

**SSE 调试**:
- [SSE Client (Chrome 插件)](https://chrome.google.com/webstore/detail/sse-client)
- [Hoppscotch](https://hoppscotch.io/)

---

## 高级场景

### 场景 1: 切换员工实例

**流程**:
1. 用户点击侧边栏另一个员工
2. 调用 `POST /client/conversations` 创建新会话（使用新 `instanceId`）
3. 切换聊天窗口到新会话

**注意**:
- 每个实例的会话是独立的
- 切换实例 ≠ 切换会话

### 场景 2: 多轮对话

**流程**:
1. 发送第一条消息，获得回复
2. 继续使用相同 `conversationId` 发送后续消息
3. AI 会记住上下文

**示例**:
```bash
# 第一轮
POST /gateway/chat
{"conversationId": "conv-1", "message": "我是做母婴产品的"}

# 第二轮（AI 会记住前面说的）
POST /gateway/chat
{"conversationId": "conv-1", "message": "帮我写个文案"}
```

### 场景 3: 工具调用（网页搜索）

**员工**: 市场调研员  
**消息**: "搜索一下2024年母婴市场趋势"

**SSE 流**:
```
data: {"type":"tool_call","name":"web_search","args":{"query":"2024年母婴市场趋势"}}

data: {"type":"tool_result","name":"web_search","result":"[搜索结果...]"}

data: {"type":"text","content":"根据搜索结果..."}
```

**客户端展示**:
- 工具调用时显示"🔍 正在搜索..."
- 工具结果可折叠展示
- 最终答案正常显示

---

## 性能优化建议

### 1. 连接池复用

客户端维护 HTTP 连接池，避免每次请求都建立新连接。

### 2. Token 缓存

Token 有效期 1 小时，客户端应缓存并在过期前刷新。

### 3. 实例列表缓存

`/client/instances` 结果变化不频繁，可缓存 5 分钟。

### 4. 消息分页

历史消息超过 50 条时，使用分页：
```
GET /client/conversations/:id/messages?page=1&limit=20
```

---

## 联系方式

**问题反馈**:
- 后端 Bug: 在项目 GitHub Issues 提交
- API 疑问: 查看 `docs/对接/SEP客户端API文档.md`
- 紧急问题: 联系后端开发团队

**文档更新**:
- 最新版本: `docs/对接/客户端联调指南.md`
- 最后更新: 2026-07-31

---

**祝联调顺利！** 🎉
