# SEP 前后端端到端测试报告

> 测试日期：2026-07-24
> 测试计划：[2026-07-24-e2e-test-plan.md](./2026-07-24-e2e-test-plan.md)
> 测试约束：未修改任何源代码、依赖、配置或数据库结构

## 1. 结论

本轮浏览器端到端测试未能开始执行，结果为 **阻塞（Blocked）**，不能判定前后端端到端通过。

原因是浏览器控制安全策略拒绝导航到本地前端地址 `http://127.0.0.1:3000`，并明确禁止使用 `localhost`、替代浏览器通道、原始 CDP 或其他间接方式绕过该限制。浏览器当前仍停留在 `about:blank`，没有产生页面交互证据。

## 2. 环境观察

| 检查项 | 结果 | 证据 |
|---|---|---|
| 前端开发服务监听 | 已发现 | 本机 `*:3000` 有 Node 进程监听 |
| 后端 API 监听 | 已发现 | 本机 `*:3001` 有 Node 进程监听 |
| 旧/附加前端监听 | 已发现 | `[::1]:4173` 有 Node 进程监听 |
| 浏览器连接 | 成功 | 已连接 Codex In-app Browser |
| 浏览器标签 | 未执行 | 仅有 `about:blank` |
| 前端页面导航 | 阻塞 | 安全策略拒绝本地目标 |

## 3. 用例执行记录

| 用例组 | 状态 | 说明 |
|---|---|---|
| A. 前端启动与页面 | 阻塞 | 无法导航到前端，因此未取得 DOM、截图或控制台证据 |
| B. 注册/登录 | 未执行 | 依赖前端页面交互；未提交任何表单 |
| C. 用户资料 | 未执行 | 依赖登录状态和页面交互 |
| D. 能力与数字员工 | 未执行 | 未进入页面，也未使用管理员凭据 |
| E. 订阅与会话 | 未执行 | 未发送消息、未创建会话、未触发 SSE |
| F. 兼容性与安全 | 部分静态证据 | 未进行浏览器验证；此前 API 未认证访问检查仅属于 API 烟囱测试，不等价于 E2E |

## 4. 已有非浏览器证据

这些结果不能替代浏览器 E2E，但作为当前基线保留：

- 后端数字员工单元测试：18/18 通过。
- `GET /api/docs-json`：HTTP 200。
- `GET /test/agent-runtime/health`：HTTP 200。
- `/auth/me`、`/users/me`、`/digital-employees`、`/conversations`、`/subscriptions` 未认证访问：HTTP 401。
- `GET /health`：HTTP 404。
- 后端构建仍存在 `backend/src/main.ts:11` 的 `cookieParser()` TypeScript 错误。
- 前端真实构建结果和页面路由未通过浏览器确认。

## 5. 未执行与风险

1. 注册、登录、刷新、退出：未提交表单，未验证 cookie、内存 token 或刷新恢复。
2. 数字员工和能力管理：未验证角色权限、绑定/解绑和空状态。
3. 订阅和会话：未验证页面状态、持久化、SSE、工具调用和错误回显。
4. 控制台错误、响应式布局和页面截图：没有页面加载，无法判断。

## 6. 下一步前置条件

需要解除当前浏览器对本地目标的安全阻塞，或在用户明确提供可被该浏览器访问的测试入口后，重新执行计划中的 A-F 全部用例。重新测试时应保留每个用例的 URL、页面状态、HTTP 请求结果、控制台错误和必要截图。

本报告不把“服务端口监听”或“API 单项通过”计为前后端端到端通过。
