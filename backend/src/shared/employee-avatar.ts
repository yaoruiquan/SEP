/** Platform-owned avatar identity and explicitly supported compositions. */
export interface EmployeeAvatarAsset {
  id: string;
  version: string | null;
  portraitUrl: string;
  faceUrl: string;
}
